"""USDJPY entry latency attribution helpers.

This package is read-only. It turns existing runtime evidence into an operator
timeline so slow entry can be diagnosed without adding any execution surface.
"""
